#ifndef POLYGON_HPP
#define POLYGON_HPP

#include <vector>

class Polygon {
public:
    // Function to sort points based on X coordinate
    static void sortPoints(std::vector<std::pair<double, double>>& points);

    // Add any other member functions or data members as needed
};

#endif // POLYGON_HPP
